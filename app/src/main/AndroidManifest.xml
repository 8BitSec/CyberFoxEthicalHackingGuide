<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apps.cyberfox_ethicalhackingguide">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Linux_Cmds"></activity>
        <activity android:name=".StartLearning" />
        <activity android:name=".Learn_Linux" />
        <activity android:name=".Introductin_to_EH" />
        <activity android:name=".CyberLaws" />
        <activity
            android:name=".MainActivity"
            android:label="CyberFox">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>